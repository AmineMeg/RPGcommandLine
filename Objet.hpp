#include <iostream>
#include <vector>
#include "Personnage.cpp"
using namespace std;

class Objet{
    private:

    public:
        void setPersonnage(Personnage p);
        Personnage getProprietaire();
};
